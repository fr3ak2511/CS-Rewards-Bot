# ── OS Generated Files ────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Python Caches & Virtual Environments ──────────────────────────────────────
__pycache__/
*.pyc
*.pyo
venv/
env/

# ── Secrets ───────────────────────────────────────────────────────────────────
.env

# ── Bot Runtime & State Files ─────────────────────────────────────────────────
# These are managed by the bot and committed back via GitHub Actions.
# They should NOT be manually edited or committed by hand.
claim_history.json
bot_meta.json

# ── Bot Debug Outputs ─────────────────────────────────────────────────────────
# Screenshots and HTML logs generated during each run.
# Uploaded as GitHub Actions artifacts and auto-deleted after 1 day.
*.png
*.html

# ── Legacy Files (safe to delete from repo) ───────────────────────────────────
# send_email_with_log.py  — references a script that no longer exists
# store_claims_log.csv    — replaced by claim_history.json
