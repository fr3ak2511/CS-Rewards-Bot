name: Hub Progression Program Rewards

on:
  schedule:
    - cron: "30 13 * * *"  # 7:00 PM IST
    - cron: "0 17 * * *"   # 10:30 PM IST
  workflow_dispatch:

jobs:
  run-progression-rewards:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install selenium

      - name: Run script and email log
        env:
          SMTP_HOST: ${{ secrets.SMTP_HOST }}
          SMTP_PORT: ${{ secrets.SMTP_PORT }}
          SMTP_USER: ${{ secrets.SMTP_USER }}
          SMTP_PASS: ${{ secrets.SMTP_PASS }}
          EMAIL_FROM: ${{ secrets.EMAIL_FROM }}
          EMAIL_TO: ${{ secrets.EMAIL_TO }}
        run: |
          python send_email_with_log.py 2_hub_ProgressionProgram_rewards.py "Hub Progression Program Rewards"
